# Production configuration
# DO NOT use VITE_API_TOKEN in production - use Twitch OAuth instead
# The application will use Twitch OAuth for authentication

# URL del discovery service (servizio centrale per mappare user_id -> tunnel_url)
# Questo servizio mantiene la mappatura tra user_id Twitch e l'URL del tunnel del server locale
# Il discovery service restituisce anche il client_id Twitch del server locale
VITE_DISCOVERY_SERVICE=https://discovery.myhomegames.com

# API base URL (opzionale, sarà sostituito dinamicamente dal tunnel_url ottenuto dal discovery service)
# Può essere rimosso o lasciato vuoto se si usa solo il discovery service
# L'applicazione otterrà l'URL del server locale dinamicamente dopo il login Twitch
VITE_API_BASE=
