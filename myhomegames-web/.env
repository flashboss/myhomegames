# Production configuration
# DO NOT use VITE_API_TOKEN in production - use Twitch OAuth instead
# The application will use Twitch OAuth for authentication

# API base URL (production)
# Set this to your production API URL (e.g., https://api.yourdomain.com)
VITE_API_BASE=https://your-api-domain.com

# Development configuration notes:
# For development (0-X-SNAPSHOT branch), use:
# VITE_API_TOKEN=changeme (for development authentication when Twitch OAuth is not configured)
# VITE_API_BASE=http://127.0.0.1:4000 (for local development server)
# The application will not start without VITE_API_BASE configured
